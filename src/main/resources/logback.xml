<?xml version="1.0" encoding="UTF-8"?>


<!-- 
Logging levels in order of logging hierarchy descending order__

FATAL -	Designates very severe error events that will presumably lead the application to 		abort. <br/>

ERROR -	Designates error events that might still allow the application to continue running.

INFO -	Designates informational messages that highlight the progress of the application at 		coarse-grained level.

DEBUG -	Designates fine-grained informational events that are most useful to debug an 			application.

TRACE -	Designates finer-grained informational events than the DEBUG.

 -->
<configuration>

	<appender name="fileAppender" class="ch.qos.logback.core.FileAppender">
		<!-- creates a folder named log in the current directory and pushes the 
			springlogs file there . -->
		<file>logs/springlogs.log</file>
		<append>true</append>
		<encoder>
			<!-- pattern followed -[DEBUG][DATE and TIME][Class-line][THREAD][MSG] [number of lines of exception] -->
			<pattern>[%-5level] [%d] [%logger{35} - %L] [%thread ] - %msg %n  %throwable{6}
			</pattern>
		</encoder>
	</appender>

	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>[%-5level] [%d] [%logger{35} - %L] [%thread ] - %msg %n
			</pattern>
		</encoder>
	</appender>

	<!-- redirects all other logs for the application to file with level 
		DEBUG -->
	 <root level="DEBUG">
		<appender-ref ref="fileAppender" />
	</root> 
	
	<!-- redirects all other logs for the application to file with level 
		INFO 
	 <root level="INFO">
		<appender-ref ref="fileAppender" />
	</root> -->
	


	<!-- redirects all other logs for the application to console with level TRACE 

	<root level="TRACE">
		<appender-ref ref="stdout" />
	</root>-->

	<!-- redirects the logs in this package to file mentioned in appender . We can define the logging at the package level.-->
	<!-- <logger name="com.cisco.dft.sda.api" level="TRACE" additivity="false"> 
		<appender-ref ref="fileAppender" /> </logger> -->


</configuration>